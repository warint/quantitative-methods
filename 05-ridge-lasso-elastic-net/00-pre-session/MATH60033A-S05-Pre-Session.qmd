---
title: "Session 5: Before the session"
subtitle: "When is a deliberately biased estimator the better one?"
author: "Thierry Warin, PhD"
institute: "HEC Montréal"
course-session: "05"
format:
  revealjs:
    theme: [default, ../../slides/economist.scss]
    embed-resources: true
    slide-number: c/t
    slide-level: 2
    history: false
    transition: none
    navigation-mode: linear
    controls: true
    progress: true
    width: 1280
    height: 760
    margin: 0.06
    smaller: true
    footer: "MATH60033A · Session 05 pre-session · Thierry Warin, PhD · HEC Montréal"
---

<!-- BEGIN deck-frontmatter · scripts/build_deck_frontmatter.py -->

## Outline · Before class {.center .scrollable}

1. What to do, and how long it takes
2. Read the paper
3. Get BOTH datasets
4. 2b · The course data
5. Self-check
6. What the session will ask of you
7. Running the code

## What session 5 is for {.scrollable}
::::: columns

:::: {.column width="33%"}

### The mathematics

- Derive the ridge estimator and show how it shrinks the **SVD spectrum**
- Explain geometrically why the lasso produces exact zeros and ridge does not
- State the **elastic net** objective and what $\ell_2$ adds under correlation
- Derive the coordinate-descent update, including **soft-thresholding**

::::

:::: {.column width="33%"}

### The Python

- Standardise before penalising, with `StandardScaler` inside a pipeline
- Choose $(\alpha,\lambda)$ with `RidgeCV`, `LassoCV`, `ElasticNetCV`
- Read a coefficient path and a cross-validation curve

::::

:::: {.column width="33%"}

### The international business

- Work with more candidate predictors than countries
- Say why a variable the lasso dropped is not thereby irrelevant
- Report a penalised model honestly, including what selection did

::::

:::::

<!-- END deck-frontmatter -->
# Before the session {background-color="#E3120B" style="color:#F7F4ED"}

::: {style="margin-top: 1.0em; color:#F7F4ED"}
### When is a deliberately biased estimator the better one?
:::

## What to do, and how long it takes

| | | |
|---|---|---|
| **1** | Read the paper | 45–60 min |
| **2** | Get **both** datasets — the paper's, and your own angle | 10 min |
| **3** | Attempt the self-check | 15 min |

::: {.warn}
The lecture assumes all three are done. The practice assumes the data is already cached on your
machine.
:::

## 1 · Read the paper

**Frandi et al. (2016), *Fast and Scalable Lasso via Stochastic Frank-Wolfe Methods with a Convergence Guarantee***

::: {.muted}
[Read the article](https://doi.org/10.1007/s10994-016-5578-4)
:::

Read for the **argument**, not for coverage. Annotate four things:

| | |
|---|---|
| **1** | The research question, in one sentence |
| **2** | The method or design, and why they chose it |
| **3** | The strongest single piece of evidence |
| **4** | One limitation you would raise as a referee |

## 2 · Get BOTH datasets

::: {.warn}
The practice uses two: the **paper's data**, to reproduce one of its results, and **your own
angle**, to apply the method. Have both before you arrive.
:::

**a) The paper's replication package** — Harvard Dataverse

::: {.step}
[10.7910/DVN/QJEUKR](https://doi.org/10.7910/DVN/QJEUKR)
:::

Unzip it into `05-ridge-lasso-elastic-net/data/replication/` — that folder is git-ignored, so nothing large is
committed. Keep the authors' own structure.

## 2b · The course data

```python
import qmib

data = qmib.load("core")     # what the lecture uses
core = qmib.load("core")                # shared by every group
mine = qmib.load("angle_c_country")     # YOUR angle — see your dictionary

print(data.shape, core.shape, mine.shape)
```

::: {.check}
Run this **before** you arrive. It downloads once and caches as parquet, so the practice works
whatever the room's wifi is doing.
:::

::: {.muted}
A wide slice of the spine — more candidate indicators than usable rows · everything available: `qmib.catalog()`
:::

## 3 · Self-check

Answer on paper. If you cannot, that is what the lecture is for.

::: {.tight}
1. In one sentence: what does this session's method let you claim that the previous one did not?
2. What must be true of your data for it to apply?
3. Which claim in the paper rests on this method — and how hard does it lean on it?
:::

## What the session will ask of you

By the end you should be able to:

::: {.tight}
- State the ridge and lasso objectives and say **what each penalty buys**
- Explain why ridge shrinkage is **targeted** rather than blunt
- Derive **soft-thresholding** and use it to explain the lasso's exact zeros
- Choose between lasso and elastic net from the **correlation structure**
- Report $\lambda$ by cross-validation, and know why post-selection inference is invalid
:::

::: {.get}
**Arrive with:** the paper read and annotated · the replication package unzipped ·
`qmib.load()` run at least once · your self-check answers on paper.
:::

## Running the code

Everything runs in the **VS Codium terminal**, with the project environment active:

```bash
source .venv/bin/activate        # macOS / Linux
python
```

::: {.muted}
Missing a package? `pip install -r requirements.txt` from the repository root.
:::

::: {.muted}
Session 05: [`README.md`](../README.md) ·
the lecture: [`01-lecture/`](../01-lecture/README.md)
:::
