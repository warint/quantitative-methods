# Quantitative Methods in International Business
#
# Python 3.12. Not 3.13, not 3.14: everything below installs on those, but
# aider-chat declares requires-python <3.13 and the course uses one interpreter
# for everything. 3.12 is supported until October 2028.
#
# Install:  pip install -r requirements.txt
# Verify:   python 01-foundations-scenarios-and-tools/00-pre-session/verify_environment.py

# ---- Core (required from Session 1) ----------------------------------------
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
statsmodels>=0.14
matplotlib>=3.8
pyarrow>=15.0            # parquet caching — data are read locally, never re-downloaded

# ---- Visualisation (Sessions 08-11) -----------------------------------------
seaborn>=0.13

# ---- Gradient boosting (Session 07) -----------------------------------------
xgboost>=2.0
lightgbm>=4.1

# ---- Model interpretation (Session 07) --------------------------------------
shap>=0.44

# ---- Text as data (Session 09) ---------------------------------------------
# TF-IDF comes from scikit-learn. Embeddings are generated locally via Ollama
# (`ollama pull nomic-embed-text`), called over HTTP — no extra package required.
nltk>=3.8
# scikit-learn-extra was here for k-medoids and was never imported. It is also
# the one package that stops at cp312, so removing it is what lets the rest of
# this file install on any modern Python.

# ---- Causal machine learning (Session 10) ----------------------------------
doubleml>=0.7
econml>=0.15

# ---- Structural equation modelling (Session 09) ------------------------------
# The Python counterpart to lavaan. Reproduces lavaan's canonical results on
# HolzingerSwineford1939 and PoliticalDemocracy, both of which it ships.
semopy>=2.3

# ---- Utilities --------------------------------------------------------------
tqdm>=4.66
requests>=2.31

# ---- Linting ----------------------------------------------------------------
# Configured in ruff.toml at the repository root.  Run:  ruff check .
ruff>=0.16

# ---- Building the book and the decks ----------------------------------------
# Not needed to follow the course. Quarto drives a Jupyter kernel for the
# chapters that execute code, and reads its own YAML through the same
# interpreter.  Run:  scripts/render_book.sh
jupyter-client>=8.0
ipykernel>=6.25
nbclient>=0.9
nbformat>=5.9
PyYAML>=6.0
