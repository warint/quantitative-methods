<!-- Generated by build_solution_slides.py from ../02-practice/starter/r1_fraiberger_2021.py.
     Do not edit: change the script, then re-run the builder. -->

## The script, to copy {.scrollable}

In VS Codium: **File → New File…** → *Python File*, paste, **save it inside the
`quantitative-methods` folder** (for example as `r1.py`), and press **Run** ▷. It is the same
file as `02-practice/starter/r1_fraiberger_2021.py`.

```python
"""
Replication 1 · Session 02 · Fraiberger, Lee, Puy & Ranciere (2021)
*Media sentiment and international asset prices*

Step-by-step guide: 07-replication-workshop/02-practice/replications/R1-fraiberger-2021.md

Run it in VS Codium with the Run button (the triangle, top right), or from the
repository root in the terminal:

    python 07-replication-workshop/02-practice/starter/r1_fraiberger_2021.py

Work one step at a time: select a step's lines and press Shift+Enter to run only
those, read the output, and compare it with "You should see" in the guide.
"""

from pathlib import Path
import os
import sys

# Work from the repository root — the folder that holds qmib.py — wherever this
# file is saved and however it is run (Run button, terminal, or Shift+Enter).
starts = [Path.cwd()] + ([Path(__file__).resolve().parent] if "__file__" in globals() else [])
ROOT = next((p for s in starts for p in [s, *s.parents] if (p / "qmib.py").exists()), None)
if ROOT is None:
    sys.exit("Open the quantitative-methods folder in VS Codium (File > Open Folder) and run again.")
os.chdir(ROOT)
sys.path.insert(0, str(ROOT))
OUTPUT = Path("07-replication-workshop/02-practice/output")   # figures land here
OUTPUT.mkdir(parents=True, exist_ok=True)

import numpy as np
import pandas as pd
from scipy import stats
import matplotlib.pyplot as plt
import statsmodels.formula.api as smf

# Step 1 · Load only the columns we need from the authors' regression file
# The .dta has 119 columns; asking for a subset keeps it fast.
path = "02-exploratory-data-analysis/data/replication/public/data/regression_sample.dta"
lags = range(1, 9)                                   # the paper uses 8 lags
controls = ["sent1_best", "art1_best", "returns", "vlm_ma60_adj", "vol_ma60", "vix"]
lag_cols = [f"L{i}_{v}" for v in controls for i in lags]
cols = ["iso", "DATE", "sent1_best", "returns", "F0_index", "dow2", "dow3", "dow4", "dow5"] + lag_cols
d = pd.read_stata(path, columns=cols)
print("rows, columns:", d.shape)
print("countries:", d["iso"].nunique(), "| dates:", d["DATE"].min().date(), "to", d["DATE"].max().date())

# Step 2 · What is one row? A country-day, including weekends. Count what is missing.
d["weekday"] = d["DATE"].dt.dayofweek               # 0 = Monday ... 6 = Sunday
d["weekend"] = d["weekday"] >= 5
print(d[["sent1_best", "returns"]].notna().sum())
print(pd.crosstab(d["weekend"], d["returns"].notna(), rownames=["weekend"], colnames=["has return"]))
s = d["sent1_best"].dropna()                         # the sentiment index we profile
n = len(s)

# Step 3 · Centre: mean, 5% trimmed mean, median
print(f"n = {n:,}")
print(f"mean     {s.mean():+.3f}")
print(f"trim 5%  {stats.trim_mean(s, 0.05):+.3f}")
print(f"median   {s.median():+.3f}")

# Step 4 · Spread: standard deviation, IQR, and the empirical rule
print(f"sd  {s.std(ddof=1):.3f}   IQR  {s.quantile(0.75) - s.quantile(0.25):.3f}"
      f"   min {s.min():.2f}   max {s.max():.2f}")
for k, normal in ((1, 0.68), (2, 0.95), (3, 0.997)):
    share = ((s - s.mean()).abs() <= k * s.std()).mean()
    print(f"within {k} sd: {share:.1%}   (normal: {normal:.1%})")
print(d.groupby("iso")["sent1_best"].agg(["mean", "std"]).round(3).head(5))   # z-scored by country

# Step 5 · Shape: skewness g1 and excess kurtosis g2, tested against the course thresholds
def shape(x):
    x = pd.Series(x).dropna().to_numpy(float)
    n, xbar, sd = len(x), x.mean(), x.std(ddof=1)
    g1 = ((x - xbar) ** 3).mean() / sd ** 3
    g2 = ((x - xbar) ** 4).mean() / sd ** 4 - 3
    print(f"n = {n:,}   g1 = {g1:+.3f} (threshold ±{2 * np.sqrt(6 / n):.3f})"
          f"   g2 = {g2:+.3f} (threshold ±{4 * np.sqrt(6 / n):.3f})")
shape(s)            # the sentiment index
shape(d["returns"])  # daily returns, for comparison

# Step 6 · Look at it: histograms of sentiment and of daily returns
fig, ax = plt.subplots(1, 2, figsize=(10, 4))
ax[0].hist(s, bins=100)
ax[0].axvline(s.mean(), color="black"); ax[0].axvline(s.median(), color="red", linestyle="--")
ax[0].set_xlabel("News sentiment (z-score within country)")
ax[1].hist(d["returns"].dropna(), bins=100, range=(-10, 10))
ax[1].set_xlabel("Daily stock-index return (%), shown from -10 to +10")
plt.savefig(OUTPUT / "fig_s02_distributions.png", dpi=120)
plt.close()

# Step 7 · The simplest return regression: today's return on yesterday's sentiment
# F0_index is today's return in %; L1_sent1_best is the previous day's sentiment z-score.
m1 = smf.ols("F0_index ~ L1_sent1_best", data=d).fit(cov_type="HC1")
print(f"slope {m1.params['L1_sent1_best']:.4f}  (se {m1.bse['L1_sent1_best']:.4f})"
      f"  n = {int(m1.nobs):,}  R2 = {m1.rsquared:.4f}")

# Step 8 · The paper's regression, with the controls the package makes available
# Country fixed effects + 8 lags of sentiment, articles, returns, volume, volatility, VIX + weekday dummies.
# Standard errors clustered by date (days are shared shocks across countries).
dd = d.dropna(subset=["F0_index"] + lag_cols).copy()
rhs = " + ".join(lag_cols) + " + dow2 + dow3 + dow4 + dow5 + C(iso)"
m2 = smf.ols("F0_index ~ " + rhs, data=dd).fit(cov_type="cluster",
                                               cov_kwds={"groups": dd["DATE"].astype("int64")})
print(f"slope {m2.params['L1_sent1_best']:.4f}  (se {m2.bse['L1_sent1_best']:.4f})  n = {int(m2.nobs):,}")

# Step 9 · Add dummies for extreme return days, as the paper does
# The package does not ship the paper's dummies, so we build them the way 5_figure_1.do defines
# a "large" move: more than 3 country standard deviations above / below the country mean.
by_country = dd.groupby("iso")["F0_index"].agg(["mean", "std"])      # one row per country
dd = dd.merge(by_country, left_on="iso", right_index=True)          # adds columns mean, std
dd["big_up"] = (dd["F0_index"] > dd["mean"] + 3 * dd["std"]).astype(int)
dd["big_down"] = (dd["F0_index"] < dd["mean"] - 3 * dd["std"]).astype(int)
m3 = smf.ols("F0_index ~ " + rhs + " + big_up + big_down", data=dd).fit(
    cov_type="cluster", cov_kwds={"groups": dd["DATE"].astype("int64")})
print(f"slope {m3.params['L1_sent1_best']:.4f}  (se {m3.bse['L1_sent1_best']:.4f})"
      f"  big_up {m3.params['big_up']:.2f}  big_down {m3.params['big_down']:.2f}")
print("days flagged:", dd["big_up"].sum(), "up,", dd["big_down"].sum(), "down")

# Step 10 · Compare with the number the authors published (results/figure_2b.csv, column 1)
print(pd.DataFrame({
    "slope on L1 sentiment": [m1.params["L1_sent1_best"], m2.params["L1_sent1_best"],
                              m3.params["L1_sent1_best"], 0.049],
    "n": [int(m1.nobs), int(m2.nobs), int(m3.nobs), 102665]},
    index=["(7) simple", "(8) + FE and lags", "(9) + extreme-day dummies", "paper, Fig. 2B h=0"]).round(3))
```

## The solutions, step by step {.scrollable}

Each tab is one step, run when these slides were built: the code, then what it prints. Compare
your own output with it line by line.

:::::::: panel-tabset
### Set-up

The lines that open every script: find the repository, import what the steps use.

```{python}
#| echo: true
from pathlib import Path
import os
import sys

# Work from the repository root — the folder that holds qmib.py — wherever this
# file is saved and however it is run (Run button, terminal, or Shift+Enter).
starts = [Path.cwd()] + ([Path(__file__).resolve().parent] if "__file__" in globals() else [])
ROOT = next((p for s in starts for p in [s, *s.parents] if (p / "qmib.py").exists()), None)
if ROOT is None:
    sys.exit("Open the quantitative-methods folder in VS Codium (File > Open Folder) and run again.")
os.chdir(ROOT)
sys.path.insert(0, str(ROOT))
OUTPUT = Path("07-replication-workshop/02-practice/output")   # figures land here
OUTPUT.mkdir(parents=True, exist_ok=True)

import numpy as np
import pandas as pd
from scipy import stats
import matplotlib.pyplot as plt
import statsmodels.formula.api as smf
```

### 1

**Step 1 · Load only the columns we need from the authors' regression file**

```{python}
#| echo: true
# Step 1 · Load only the columns we need from the authors' regression file
# The .dta has 119 columns; asking for a subset keeps it fast.
path = "02-exploratory-data-analysis/data/replication/public/data/regression_sample.dta"
lags = range(1, 9)                                   # the paper uses 8 lags
controls = ["sent1_best", "art1_best", "returns", "vlm_ma60_adj", "vol_ma60", "vix"]
lag_cols = [f"L{i}_{v}" for v in controls for i in lags]
cols = ["iso", "DATE", "sent1_best", "returns", "F0_index", "dow2", "dow3", "dow4", "dow5"] + lag_cols
d = pd.read_stata(path, columns=cols)
print("rows, columns:", d.shape)
print("countries:", d["iso"].nunique(), "| dates:", d["DATE"].min().date(), "to", d["DATE"].max().date())
```

### 2

**Step 2 · What is one row? A country-day, including weekends. Count what is missing.**

```{python}
#| echo: true
# Step 2 · What is one row? A country-day, including weekends. Count what is missing.
d["weekday"] = d["DATE"].dt.dayofweek               # 0 = Monday ... 6 = Sunday
d["weekend"] = d["weekday"] >= 5
print(d[["sent1_best", "returns"]].notna().sum())
print(pd.crosstab(d["weekend"], d["returns"].notna(), rownames=["weekend"], colnames=["has return"]))
s = d["sent1_best"].dropna()                         # the sentiment index we profile
n = len(s)
```

### 3

**Step 3 · Centre: mean, 5% trimmed mean, median**

```{python}
#| echo: true
# Step 3 · Centre: mean, 5% trimmed mean, median
print(f"n = {n:,}")
print(f"mean     {s.mean():+.3f}")
print(f"trim 5%  {stats.trim_mean(s, 0.05):+.3f}")
print(f"median   {s.median():+.3f}")
```

### 4

**Step 4 · Spread: standard deviation, IQR, and the empirical rule**

```{python}
#| echo: true
# Step 4 · Spread: standard deviation, IQR, and the empirical rule
print(f"sd  {s.std(ddof=1):.3f}   IQR  {s.quantile(0.75) - s.quantile(0.25):.3f}"
      f"   min {s.min():.2f}   max {s.max():.2f}")
for k, normal in ((1, 0.68), (2, 0.95), (3, 0.997)):
    share = ((s - s.mean()).abs() <= k * s.std()).mean()
    print(f"within {k} sd: {share:.1%}   (normal: {normal:.1%})")
print(d.groupby("iso")["sent1_best"].agg(["mean", "std"]).round(3).head(5))   # z-scored by country
```

### 5

**Step 5 · Shape: skewness g1 and excess kurtosis g2, tested against the course thresholds**

```{python}
#| echo: true
# Step 5 · Shape: skewness g1 and excess kurtosis g2, tested against the course thresholds
def shape(x):
    x = pd.Series(x).dropna().to_numpy(float)
    n, xbar, sd = len(x), x.mean(), x.std(ddof=1)
    g1 = ((x - xbar) ** 3).mean() / sd ** 3
    g2 = ((x - xbar) ** 4).mean() / sd ** 4 - 3
    print(f"n = {n:,}   g1 = {g1:+.3f} (threshold ±{2 * np.sqrt(6 / n):.3f})"
          f"   g2 = {g2:+.3f} (threshold ±{4 * np.sqrt(6 / n):.3f})")
shape(s)            # the sentiment index
shape(d["returns"])  # daily returns, for comparison
```

### 6

**Step 6 · Look at it: histograms of sentiment and of daily returns**

```{python}
#| echo: true
# Step 6 · Look at it: histograms of sentiment and of daily returns
fig, ax = plt.subplots(1, 2, figsize=(10, 4))
ax[0].hist(s, bins=100)
ax[0].axvline(s.mean(), color="black"); ax[0].axvline(s.median(), color="red", linestyle="--")
ax[0].set_xlabel("News sentiment (z-score within country)")
ax[1].hist(d["returns"].dropna(), bins=100, range=(-10, 10))
ax[1].set_xlabel("Daily stock-index return (%), shown from -10 to +10")
plt.savefig(OUTPUT / "fig_s02_distributions.png", dpi=120)
plt.close()
```

### 7

**Step 7 · The simplest return regression: today's return on yesterday's sentiment**

```{python}
#| echo: true
# Step 7 · The simplest return regression: today's return on yesterday's sentiment
# F0_index is today's return in %; L1_sent1_best is the previous day's sentiment z-score.
m1 = smf.ols("F0_index ~ L1_sent1_best", data=d).fit(cov_type="HC1")
print(f"slope {m1.params['L1_sent1_best']:.4f}  (se {m1.bse['L1_sent1_best']:.4f})"
      f"  n = {int(m1.nobs):,}  R2 = {m1.rsquared:.4f}")
```

### 8

**Step 8 · The paper's regression, with the controls the package makes available**

```{python}
#| echo: true
# Step 8 · The paper's regression, with the controls the package makes available
# Country fixed effects + 8 lags of sentiment, articles, returns, volume, volatility, VIX + weekday dummies.
# Standard errors clustered by date (days are shared shocks across countries).
dd = d.dropna(subset=["F0_index"] + lag_cols).copy()
rhs = " + ".join(lag_cols) + " + dow2 + dow3 + dow4 + dow5 + C(iso)"
m2 = smf.ols("F0_index ~ " + rhs, data=dd).fit(cov_type="cluster",
                                               cov_kwds={"groups": dd["DATE"].astype("int64")})
print(f"slope {m2.params['L1_sent1_best']:.4f}  (se {m2.bse['L1_sent1_best']:.4f})  n = {int(m2.nobs):,}")
```

### 9

**Step 9 · Add dummies for extreme return days, as the paper does**

```{python}
#| echo: true
# Step 9 · Add dummies for extreme return days, as the paper does
# The package does not ship the paper's dummies, so we build them the way 5_figure_1.do defines
# a "large" move: more than 3 country standard deviations above / below the country mean.
by_country = dd.groupby("iso")["F0_index"].agg(["mean", "std"])      # one row per country
dd = dd.merge(by_country, left_on="iso", right_index=True)          # adds columns mean, std
dd["big_up"] = (dd["F0_index"] > dd["mean"] + 3 * dd["std"]).astype(int)
dd["big_down"] = (dd["F0_index"] < dd["mean"] - 3 * dd["std"]).astype(int)
m3 = smf.ols("F0_index ~ " + rhs + " + big_up + big_down", data=dd).fit(
    cov_type="cluster", cov_kwds={"groups": dd["DATE"].astype("int64")})
print(f"slope {m3.params['L1_sent1_best']:.4f}  (se {m3.bse['L1_sent1_best']:.4f})"
      f"  big_up {m3.params['big_up']:.2f}  big_down {m3.params['big_down']:.2f}")
print("days flagged:", dd["big_up"].sum(), "up,", dd["big_down"].sum(), "down")
```

### 10

**Step 10 · Compare with the number the authors published (results/figure_2b.csv, column 1)**

```{python}
#| echo: true
# Step 10 · Compare with the number the authors published (results/figure_2b.csv, column 1)
print(pd.DataFrame({
    "slope on L1 sentiment": [m1.params["L1_sent1_best"], m2.params["L1_sent1_best"],
                              m3.params["L1_sent1_best"], 0.049],
    "n": [int(m1.nobs), int(m2.nobs), int(m3.nobs), 102665]},
    index=["(7) simple", "(8) + FE and lags", "(9) + extreme-day dummies", "paper, Fig. 2B h=0"]).round(3))
```
::::::::
